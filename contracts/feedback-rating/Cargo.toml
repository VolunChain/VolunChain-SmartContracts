[package]
name = "feedback-rating"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]
doctest = false

[dependencies]
soroban-sdk = { version = "22.0.2", features = ["alloc"] }

[dev-dependencies]
soroban-sdk = { version = "22.0.2", features = ["testutils"] }